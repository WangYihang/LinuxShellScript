目标 : 
```
主要掌握 if 语句的多种语法
```
备注 : 
```
由于shell中所有的变量都是字符串 , 因此在 if 语句中的比较可以分为如下三种类型 : 
1. 纯粹的字符串比较
2. 数值的比较
3. 判断文件的属性(判断是否是文件 / 目录 ... )
```
语法 : 
```
##########################
if [ -f /etc/issue] ; then
	# some code
fi
##########################
if test -f /etc/issue
then
	# some code
fi
```
