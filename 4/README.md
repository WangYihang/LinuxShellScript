功能 : 
```
实现shell脚本循环读取当前目录下所有文件的功能
主要用于遍历项目中的模块文件提供选项给用户调用
```
备注 : 
```
现在还存在一个问题就是 : 
	现在排除脚本本身的方式是硬编码
	但是正常情况下应该是自动获取脚本的文件名 , 然后进行对应的排除
	但是如果使用 $0 的话 , 如果运行 : bash myls.sh , 就可以正常排除掉
	如果使用 ./myls.sh 就无法正常排除 , 因为这种情况下 $0 = ./myls.sh (即存在前面的 ./)
```
